import styles from "SkeletonFilterItem.module.css";

const SkeletonFilterItem = function () {
  const { skeletonItem, skeletonItem__Icon, skeletonItem__Text } = styles;
  return <div></div>;
};

export default SkeletonFilterItem;
